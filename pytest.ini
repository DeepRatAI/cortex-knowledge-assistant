[pytest]
markers =
    slow: slow tests that should not run on lightweight environments by default
    e2e: end-to-end tests (may require external services)
    streaming: streaming/SSE tests (optional)
