# Example secret manifest. NEVER commit real values here.
apiVersion: v1
kind: Secret
metadata:
  name: cka-secrets
  labels:
    app: cka-api
  # type: Opaque is fine; some platforms may prefer kubernetes.io/basic-auth, etc.
  type: Opaque
stringData:
  # These are placeholders. In real clusters, inject via your secret manager
  # and never store real tokens in Git.
  HF_API_KEY: "hf_example_token_change_me"
  CKA_API_KEY: "cka_example_api_key_change_me"
  CKA_QDRANT_API_KEY: "qdrant_example_api_key_change_me"
